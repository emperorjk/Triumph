namespace Assets.Scripts.Players
{
    public enum PlayerIndex
    {
        // Not entirely sure if Neutral belongs in here. I think so. Will see later.
        Neutral = 0,
        Blue = 1,
        Red = 2
    }
}